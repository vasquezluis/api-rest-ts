// archivo para encriptar contrase;as con bcruptjs

import { hash, compare } from "bcryptjs";

/**
 * ! Encriptar contrase;a
 */
const encrypt = () => {};

/**
 * ! verificar contrase;a plana con contrase;a encriptada
 */
const verified = () => {};

export { encrypt, verified };
